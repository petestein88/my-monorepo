# IoT Frontend

Frontend application for interacting with the IoT backend services.

## Getting Started

```bash
npm install
npm start
```

Opens a local development server serving the `public/` directory.

## Structure

- `public/` — Static HTML/CSS/JS served to the browser
- `src/` — Shared utility modules

## Running Tests

```bash
npm test
```
