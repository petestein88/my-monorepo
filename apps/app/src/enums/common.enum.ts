export enum STATE_ENUM {
    IDLE = 'idle',
    LOADING = 'loading',
    FULFILLED = 'fulfilled',
    FAILED = 'failed'
}

export enum ACTION {
    DEFAULT = 'DEFAULT'
}

export enum LOCAL_STORAGE_ENUM {
    OPEN_ADD_SLOT_MODAL = 'open-add-slot-modal',
    AUTH_TOKEN = 'authToken'
}
